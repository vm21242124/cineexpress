apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: cinevision
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "12345"
            - name: POSTGRES_DB
              value: "cine_vision"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: cinevision
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
